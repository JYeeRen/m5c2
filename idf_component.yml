dependencies:
  # 已有的LVGL依赖
  lvgl/lvgl:
    version: "^9.3.0"
    rules:
      - if: "idf_version >=4.4.0"
  
  # 添加其他常用组件
  espressif/esp_lcd_touch:
    version: "^1.1.0"
    rules:
      - if: "idf_version >=4.4.0"
  
  espressif/esp_lcd_ili9341:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=4.4.0"
  
  espressif/esp_lcd_touch_ft5x06:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=4.4.0" 